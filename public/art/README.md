Home-screen artwork. Drop files here and the home screen uses them; while a
file is missing the built-in SVG version shows instead.

- logo.png        the wordmark with the crowned toilet (transparent background)
- daily-card.png  the "Today's round" island illustration (transparent or dark background)

Keep them under ~300 KB each (1200 px wide is plenty).
